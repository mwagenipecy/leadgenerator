## Lender Frontend

Use the same folder standards as `borrower`:

- `src/auth/` for auth-only logic
- `src/router/` for page navigation routes
- `src/shared/` for reusable components
- `src/features/` for domain pages

This app should reuse the same backend (`LeadNet.Api`) and role-based route patterns.
